spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: ${POSTGRES_PASSWORD:example}
    username: ${POSTGRES_USERNAME:example}
    url: ${POSTGRES_JDBC_URL:jdbc:postgresql://localhost/kafif_linker}
  application:
    name: backend